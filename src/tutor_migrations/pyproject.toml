[project]
name = "tutor-migrations"
version = "0.1.0"
description = "Standalone SurrealDB migration toolkit for rag-tutor"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic-settings>=2.15.0",
    "surrealdb>=2.0.0",
]

[project.scripts]
tutor-migrations = "tutor_migrations.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tutor_migrations"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
